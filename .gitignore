# Node modules
frontend/node_modules/
node_modules/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDE ? track shared vscode recommendations
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
*.env.local
*.local.env
config.env

# TLS material (keep .gitkeep)
deploy/ssl/*.pem
deploy/ssl/*.key
deploy/ssl/*.crt
!deploy/ssl/.gitkeep

# Scratch / agent temp files (repo root)
.tmp_*
.tmp*/

# Release tarballs (local / CI output)
artifacts/

# Logs (do not commit runtime logs)
*.log
log/

# Database / local data
*.db
*.sqlite3
data/
!data/
data/*
!data/.gitkeep
!data/intent/
!data/intent/**
data/intent/eval/reports/
chroma_db/
uploads/

# Local assets (repo root only; do not ignore frontend/src/assets)
/assets/
.superpowers

# Internal pitch/positioning ? NOT open-source (do not commit)
docs/pitch/

# Internal launch gap analysis ? NOT open-source (do not commit)
docs/audit/2026-08-24-launch-gap-checklist.md
