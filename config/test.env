# ============================================
# test 环境 — make test / CI 使用
# 由根目录 conftest.py 设置 APP_ENV=test
# ============================================

# 测试全部走 fixture 回放:离线、确定性、零成本
LLM_PROVIDER=replay
