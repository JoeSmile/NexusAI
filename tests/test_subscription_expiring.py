"""E3.3b alias — subscription helpers (shared with grant notify tests)."""

from tests.test_grant_expiring_notify import (  # noqa: F401
    test_payload_allowlist_includes_expiry_fields,
    test_subscription_roundtrip_in_config,
)
