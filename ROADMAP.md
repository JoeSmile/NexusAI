# NexusAI Roadmap — 企业 AI 中台

> 更新:2026-08-05（含「可视化 Workflow」拍板）。  
> 定位见 [`docs/strategy/AI_MIDDLE_PLATFORM.md`](strategy/AI_MIDDLE_PLATFORM.md)。  
> **分界原则:** 修「代码已烂」尽早做；造「客户付费能力」按阶段交付，不抢跑画布与假数据。

---

## 版本线(2026-08-05 拍板)

| 版本 | 内容 | 状态 |
|------|------|------|
| **V0.0.1** | 现状基线:管线 + Capability Hub + Task 38 账号体系 + 开源门面(当前代码即此版) | ✅ 当前 |
| **V0.1.0** | **链 A(试点 B 人侧金线)+ 企业 AI 工作台 UI**(参考 [`docs/ui-reference/`](ui-reference/)) | 🚧 推进中 |

- **推进方式:** 链 A 与工作台 UI **参杂着做**(并行交错,不串行排队)。
- **参考 UI(2 张):** ① `docs/ui-reference/workbench-dashboard.png` — 工作台首页:KPI 卡 + 工作队列(带「AI 已审」+ 证据引用 + 原文链接)+ 近期证据;② `docs/ui-reference/chat-reasoning-steps.jpg` — Chat 推理步骤展示(理解→意图→检索)+ 结构化表格回答。
- **V0.1.0 目标体验:** 「把 AI 判断、证据引用、审批责任一体化——让 AI 进入企业流程,而不是又一个聊天框」。

---

## 北极星(要做成什么样)

做成更完善的 **企业 AI 中台**，让各部门员工在 **自身权限范围内**：

1. **编排并运行 Workflow** — 选模板 / 填参数 / 排步骤 / 一键跑；权限与租户隔离贯穿始终（不是人人可改生产链）。
2. **快速对接 OA 与办公场景** — 制度/流程进 RAG 与连接器；账目核对、日程与待办安排等「能办公」的闭环。
3. **企业指标可视化(尤其 AI)** — 成本、调用量、命中率、护栏拦截、模型分流、任务成功率等：图标/走势/曲线可看板化，给 CIO 与部门负责人「看得见的治理」。

> 治理仍是芯：自助编排必须可审计、可审批、可配额；可视化优先接 **已有** Prometheus / LangFuse / `audit_logs`，再扩展业务 KPI。

```text
                    ┌─ 权限内 Workflow 编排与运行
  各部门员工 ───────┤─ OA / 对账 / 日程等办公闭环
                    └─ AI 与经营指标看板(趋势·曲线)

  IT / tenant_admin ─ 能力目录、连接器、模板治理、审批与配额
  auditor           ─ 跨租户只读审计与导出
```

---

## 决策:要不要可视化 Workflow 编排系统?(2026-08-05 拍板)

**结论:要搭配，但不是「第二个 Dify」，也不是第一仗。**

| 判断 | 说明 |
|------|------|
| **中长期要有** | 各部门按权限编排并运行、对账/OA 多步 + 审批，没有可视化（至少模板流程图）业务侧用不起来，永远只能 IT 配链。 |
| **短期不重仓拖拽画布** | 编排器是红海；首屏若是画布，治理叙事被淹没，且工程量会挤掉管线可信、OA 样板、AI 看板。 |
| **差异化一句话** | 能编排，但编的每一步都在治理里（权限 / 审计 / 审批 / 配额）。可视化是**编排体验层**，执行仍走 Capability invoke + 管线 / 护栏。 |

**落地形态(与阶段绑定):**

```text
阶段一/二     无全员拖拽
              → IT 配链 + 按钮/chat 跑 + 对账样板 + AI 指标看图
              → 可选:只读流程图预览(展示已发布链，不可改)

阶段三起步    模板 + 表单式编排(选步骤、填参、排序、保存)
              → 只读/半只读流程图预览
              → 仅可见有权 capability；发布可走审批

阶段三往后    受限拖拽画布
              → 只能拖已授权节点；版本/调试/审计齐全
              → 不做「无治理的通用 workflow 套壳」
```

**明确不是:**

- 再造一套与 Hub 平行的执行引擎  
- 阶段一/二上线全员自由画布  
- 用假数据撑编排 Demo  

---

## 已完成(历史基线)

- **v1.0～v1.2** — 管线 / RAG / SSE / Admin / ModelRegistry / A-B / LangFuse / Key failover 等
- **Capability Hub (30.x)** — 注册表 + 统一 invoke + Agent 链雏形 + 外部连接器骨架
- **Task 38** — 测试 FE 密码注册/登录 → 签发 `cg_` Key
- **开源门面** — README / learning 面试深挖包；Task 39 早预处理设计稿

---

## 阶段一 — V1.x 收尾(当前 · 修洞)

> 目标:自己跑一遍数据可信；为中台场景打地基。

- [ ] **管线早预处理(Task 39)** — 归一化 + cheap gate + 推迟 `load_memory`；exact 缓存读写同一 `query_hash`
- [ ] **缓存写侧对齐** — 与 Task 35/39 核对：非 mock 路径是否写 exact、命中率可测
- [ ] **实体抽取空壳** — `analyze_parallel` entities 实现或诚实摘掉「并行」表述
- [ ] **Chat 旁路收敛** — 与主管线职责重叠的入口收口
- [ ] **Agent 双轨合一** — `backend/agent` 与 `kind=agent` 叙事一致
- [ ] **journeys / MANUAL_TEST 实测** — 4 角色剧本 + 证据数字(延迟/成本/命中)

---

## 阶段二 — 中台第一仗(IT 配链 · 办公样板 · 指标可读)

> 目标:**先让业务「能用」**——IT/管理员配好链与模板，员工 chat 或按钮调用；打通 OA/对账样板；AI 指标先「看得见」。
> 本阶段仍 **不做** 全员自由拖拽画布(画布在阶段三按权限开放)。

### 2.1 调用打通

- [ ] **chat → 能力链桥接** — skill / intent 可触发 capability chain(现 chat 与 Hub 两路断开)
- [ ] **工作台按钮页** — 按钮 → 直连 `invoke`(确定动作、$0 优先)

### 2.2 办公与数据

- [ ] **OA 连接器(起步)** — 导入 + 定时同步；租户/角色/行级隔离；不先啃全量实时接口
- [ ] **账目核对样板链** — 拉数 → RAG 查制度 → LLM 辅助计算 → **人工审批** → 报告
- [ ] **安排/待办雏形** — 与日程/任务类工具对接或最小内建(可后置，样板链优先)

### 2.3 AI 指标可视化(MVP)

> 先消费现有信号，不新造「假大屏」。

- [ ] **运营看板 v0** — 租户维度:请求量、成本、token、缓存命中、护栏拦截、短/长路径占比
  数据源:`/metrics`(Prometheus)、LangFuse 抽样、`audit_logs` 聚合
- [ ] **趋势与曲线** — 日/小时粒度折线(成本、调用、错误码)；导出 CSV 给 auditor
- [ ] **测试 FE Performance 面板升级** — 从单卡数字 → 简单走势图(后续迁产品 FE)

---

## 阶段三 — 完善中台(权限内自助编排 + 场景深化 + 可视化加强)

> 目标:各部门员工在 **RBAC + 能力权限** 内编排、保存、运行自己的 workflow；OA/对账场景产品化；AI 与业务指标看板达到「汇报可用」。

### 3.1 权限内 Workflow(含可视化编排台)

> 依据上文「可视化 Workflow」拍板:先表单编排 + 流程图预览，再受限画布。

- [ ] **自助编排(模板 + 表单优先)** — 选模板、填参数、排顺序、保存为个人/部门链
- [ ] **流程图预览** — 已保存/已发布链的只读可视化(非编辑画布)
- [ ] **受限拖拽画布(后置)** — 仅拖已授权 capability；版本与调试；禁止无权限节点
- [ ] **运行与权限闸** — 仅可见/可调自己有权的 capability；跨部门链需审批或 admin 发布
- [ ] **审批节点** — 对账报告 / 采购类流程「领导批」；与现有 `/permissions` 审批雏形打通
- [ ] **产品 FE(原 30.29)** — 管理控制台 + 工作台 + **编排台**；人侧会话凭证、机器侧 API Key 拆分

### 3.2 办公闭环加深

- [ ] **OA / 财务连接器加固** — 更多对象类型、失败重试、同步水位、权限映射
- [ ] **对账 / 制度问答样板间** — 可演示、可私有化交付的标准包
- [ ] **数据不出域套件** — 本地模型 + 私有化部署清单(国企刚需)

### 3.3 可视化与治理产品化

- [ ] **预算报表** — 单次/日/月限额 + 审批两本账(原 Task 32 设计)
- [ ] **护栏配置化** — 租户级规则，差异化卖点
- [ ] **AI 治理大盘** — 模型分流、key 健康/failover、采样率、部门成本排行、异常告警
- [ ] **业务 KPI 扩展** — 对账完成率、审批时长、OA 同步延迟等(随连接器成熟接入)

---

## 架构深化(随做随补)

- [ ] 凭证拆分(`credential_type`:机器 key vs 人类会话) — 设计见 [`docs/superpowers/specs/2026-08-05-enterprise-pilot-b-gaps-design.md`](superpowers/specs/2026-08-05-enterprise-pilot-b-gaps-design.md) **§9**（J0.5/J0.6）
- [ ] 统一上游管理面(Harness + 外部 connector 状态看板)
- [ ] **连接器 Provider 化加载(借鉴 AtlasClaw providers_root)** — capability 连接器动态包加载,见 [`COMPETITIVE_ANALYSIS.md`](COMPETITIVE_ANALYSIS.md) §6.4
- [ ] **嵌入式浮窗模式(借鉴 AtlasClaw,后期)** — 嵌入现有 OA/门户:菜单 UI + 浮窗 UI 共享会话,浮窗绑定当前页面业务对象;国企强场景,见 §6.4
- [ ] **证据一等公民(借鉴 AtlasClaw 工作队列,链 A P0)** — 节点产出 `evidence[]`;工作台「AI 已审」可点开引用/原文;见 COMPETITIVE §6.6
- [ ] **context_ref 契约预留** — plan/run/handoff 可带业务对象引用,为嵌入浮窗铺路(实现可后置)
- [ ] Prompt 版本管理与回滚
- [ ] 任务中断恢复 / SSE 续传
- [ ] 多 Agent 并发与节点独立超时
- [ ] 流式路径与非流式 Harness 能力对齐(key failover 等，见 docs/archive/learning/07c)

---

## 内容与传播

- [ ] 证据包(MANUAL_TEST 全路线数字)
- [ ] 技术文章第一篇(治理 / 护栏 / 审计任选)
- [ ] 简历与售前一页纸(PROJECT_SUMMARY + JOB_HUNT)

---

## 本阶段明确不做 / 缓做

| 不做或缓做 | 原因 |
|------------|------|
| 阶段一/二上全员自由拖拽画布 | 见「可视化 Workflow」拍板；先模板+按钮+样板链 |
| 再造平行执行引擎 / 第二个 Dify | 编排体验层挂现有 Hub；治理是芯 |
| 造假 demo 数据美化大屏或编排 Demo | 实事求是；指标用真实 `/metrics` |
| 横向再铺「无治理的通用 workflow」 | 差异化在 **治理 + 权限 + 国企办公 + AI 可观测** |
| 名称反复横跳 | 市场名 NexusAI，仓库已切远端 |

---

## 阶段对照(一句话)

| 阶段 | 一句话 |
|------|--------|
| 一 | 洞补上，缓存/归一化/旁路可信 |
| 二 | IT 配链 + 按钮/chat 能跑；OA/对账样板；AI 指标能看图 |
| 三 | 员工按权限自助编排并运行；办公闭环加深；治理与 AI 大盘汇报级 |

当前建议执行顺序:**阶段一(含 Task 39)→ 阶段二可视化 MVP + chat 桥接 → 再开阶段三编排。**
